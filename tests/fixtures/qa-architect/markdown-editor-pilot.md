# QA Architect beta fixture

Use the plugin's synthetic Markdown editor pilot as the disposable integration
scenario. A valid Discovery response must ask about byte preservation versus
semantic equivalence before proposing an AST-based test, and a valid QA Audit
must report a gap if a one-line deletion mutation passes.
